<div class="container">
  <div class="cardDash p-5 shadow">
    <div class="form">
      <span><h1> Edit your Playlist Information</h1>

      <%= render "form", playlist: @playlist %>
      <%= link_to ('<i class="fa-solid fa-backward"></i>').html_safe, manage_playlist_playlist_songs_path(@playlist), class: "back" %>
    </div>
  </div>
</div>
